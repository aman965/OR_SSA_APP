# OR SaaS Application - Core Dependencies
# For production deployment, install only these core dependencies

# Core dependencies (required)
-r requirements-core.txt

# Installation Instructions:
# 
# For production:
#   pip install -r requirements.txt
# 
# For development (includes testing tools):
#   pip install -r requirements.txt -r requirements-dev.txt
# 
# For full features (includes optional performance enhancements):
#   pip install -r requirements.txt -r requirements-optional.txt
# 
# For complete development setup:
#   pip install -r requirements.txt -r requirements-dev.txt -r requirements-optional.txt 